/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 806c3e535dd1
 */

import { GleanCore } from "../core.js";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as components from "../models/components/index.js";
import { GleanBaseError } from "../models/errors/gleanbaseerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

export enum CreateRunAcceptEnum {
  applicationJson = "application/json",
  textEventStream = "text/event-stream",
}

/**
 * Create agent run
 *
 * @remarks
 * Execute an agent run. Set `stream` to true to receive server-sent events; otherwise the response contains the final agent messages.
 */
export function agentsCreateRun(
  client: GleanCore,
  platformAgentRunCreateRequest: components.PlatformAgentRunCreateRequest,
  agentId: string,
  options?: RequestOptions & { acceptHeaderOverride?: CreateRunAcceptEnum },
): APIPromise<
  Result<
    operations.PlatformAgentsCreateRunResponse,
    | errors.PlatformUnauthorizedAgentToolsProblemError
    | errors.PlatformProblemDetailError
    | GleanBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    platformAgentRunCreateRequest,
    agentId,
    options,
  ));
}

async function $do(
  client: GleanCore,
  platformAgentRunCreateRequest: components.PlatformAgentRunCreateRequest,
  agentId: string,
  options?: RequestOptions & { acceptHeaderOverride?: CreateRunAcceptEnum },
): Promise<
  [
    Result<
      operations.PlatformAgentsCreateRunResponse,
      | errors.PlatformUnauthorizedAgentToolsProblemError
      | errors.PlatformProblemDetailError
      | GleanBaseError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.PlatformAgentsCreateRunRequest = {
    platformAgentRunCreateRequest: platformAgentRunCreateRequest,
    agent_id: agentId,
  };

  const parsed = safeParse(
    input,
    (value) =>
      operations.PlatformAgentsCreateRunRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = encodeJSON("body", payload.PlatformAgentRunCreateRequest, {
    explode: true,
  });

  const pathParams = {
    agent_id: encodeSimple("agent_id", payload.agent_id, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/api/agents/{agent_id}/runs")(pathParams);

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: options?.acceptHeaderOverride
      || "application/json;q=1, text/event-stream;q=0",
  }));

  const secConfig = await extractSecurity(client._options.apiToken);
  const securityInput = secConfig == null ? {} : { apiToken: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "platform-agents-create-run",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiToken,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.PlatformAgentsCreateRunResponse,
    | errors.PlatformUnauthorizedAgentToolsProblemError
    | errors.PlatformProblemDetailError
    | GleanBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, operations.PlatformAgentsCreateRunResponse$inboundSchema),
    M.text(200, operations.PlatformAgentsCreateRunResponse$inboundSchema, {
      ctype: "text/event-stream",
    }),
    M.jsonErr(
      422,
      errors.PlatformUnauthorizedAgentToolsProblemError$inboundSchema,
      { ctype: "application/problem+json" },
    ),
    M.jsonErr(
      [400, 401, 403, 404, 408, 409, 413, 429],
      errors.PlatformProblemDetailError$inboundSchema,
      { ctype: "application/problem+json" },
    ),
    M.jsonErr([500, 503], errors.PlatformProblemDetailError$inboundSchema, {
      ctype: "application/problem+json",
    }),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
