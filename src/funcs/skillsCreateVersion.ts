/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a41608d008c9
 */

import { GleanCore } from "../core.js";
import { appendForm, encodeSimple, normalizeBlob } from "../lib/encodings.js";
import {
  bytesToBlob,
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as components from "../models/components/index.js";
import { GleanBaseError } from "../models/errors/gleanbaseerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Create skill version
 *
 * @remarks
 * Create a new immutable version for an existing caller-managed skill from an uploaded SKILL.md, .zip, or .skill bundle.
 */
export function skillsCreateVersion(
  client: GleanCore,
  platformSkillVersionCreateRequest:
    components.PlatformSkillVersionCreateRequest,
  skillId: string,
  options?: RequestOptions,
): APIPromise<
  Result<
    components.PlatformSkillVersionCreateResponse,
    | errors.PlatformProblemDetailError
    | GleanBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    platformSkillVersionCreateRequest,
    skillId,
    options,
  ));
}

async function $do(
  client: GleanCore,
  platformSkillVersionCreateRequest:
    components.PlatformSkillVersionCreateRequest,
  skillId: string,
  options?: RequestOptions,
): Promise<
  [
    Result<
      components.PlatformSkillVersionCreateResponse,
      | errors.PlatformProblemDetailError
      | GleanBaseError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.PlatformSkillsCreateVersionRequest = {
    platformSkillVersionCreateRequest: platformSkillVersionCreateRequest,
    skill_id: skillId,
  };

  const parsed = safeParse(
    input,
    (value) =>
      operations.PlatformSkillsCreateVersionRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = new FormData();

  if (isBlobLike(payload.PlatformSkillVersionCreateRequest.file)) {
    const file = payload.PlatformSkillVersionCreateRequest.file;
    const blob = await normalizeBlob(file);
    const name = "name" in file ? (file.name as string) : undefined;
    appendForm(body, "file", blob, name);
  } else if (
    isReadableStream(payload.PlatformSkillVersionCreateRequest.file.content)
  ) {
    const buffer = await readableStreamToArrayBuffer(
      payload.PlatformSkillVersionCreateRequest.file.content,
    );
    const contentType =
      getContentTypeFromFileName(
        payload.PlatformSkillVersionCreateRequest.file.fileName,
      ) || "application/octet-stream";
    appendForm(
      body,
      "file",
      bytesToBlob(buffer, contentType),
      payload.PlatformSkillVersionCreateRequest.file.fileName,
    );
  } else {
    const contentType =
      getContentTypeFromFileName(
        payload.PlatformSkillVersionCreateRequest.file.fileName,
      ) || "application/octet-stream";
    appendForm(
      body,
      "file",
      bytesToBlob(
        payload.PlatformSkillVersionCreateRequest.file.content,
        contentType,
      ),
      payload.PlatformSkillVersionCreateRequest.file.fileName,
    );
  }

  const pathParams = {
    skill_id: encodeSimple("skill_id", payload.skill_id, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/api/skills/{skill_id}/versions")(pathParams);

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.apiToken);
  const securityInput = secConfig == null ? {} : { apiToken: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "platform-skills-create-version",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiToken,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    components.PlatformSkillVersionCreateResponse,
    | errors.PlatformProblemDetailError
    | GleanBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, components.PlatformSkillVersionCreateResponse$inboundSchema),
    M.jsonErr(
      [400, 401, 403, 404, 408, 409, 413, 429],
      errors.PlatformProblemDetailError$inboundSchema,
      { ctype: "application/problem+json" },
    ),
    M.jsonErr([500, 503], errors.PlatformProblemDetailError$inboundSchema, {
      ctype: "application/problem+json",
    }),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
