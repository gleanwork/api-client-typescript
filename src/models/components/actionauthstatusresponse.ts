/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 11d10a777f20
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ActionAuthType,
  ActionAuthType$inboundSchema,
} from "./actionauthtype.js";

export type ActionAuthStatusResponse = {
  /**
   * Whether the calling user is already authenticated to the tool backing the action pack.
   */
  authenticated: boolean;
  /**
   * Authentication mechanism used by an action pack.
   *
   * @remarks
   *   - `AUTH_USER_OAUTH`: Requires per-user OAuth consent to the third-party tool.
   *   - `AUTH_ADMIN`: Uses a service-account / admin-owned credential. End users do not authorize individually.
   *   - `AUTH_NONE`: Action pack requires no authentication.
   */
  authType: ActionAuthType;
};

/** @internal */
export const ActionAuthStatusResponse$inboundSchema: z.ZodType<
  ActionAuthStatusResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  authenticated: z.boolean(),
  authType: ActionAuthType$inboundSchema,
});

export function actionAuthStatusResponseFromJSON(
  jsonString: string,
): SafeParseResult<ActionAuthStatusResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ActionAuthStatusResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ActionAuthStatusResponse' from JSON`,
  );
}
