/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 93b5dac2360e
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AuthorizeActionResponse = {
  /**
   * URL that the customer UI should navigate the end user to in order to begin the third-party OAuth flow.
   *
   * @remarks
   * After the user consents, control returns to `returnUrl` from the request.
   */
  redirectUrl: string;
};

/** @internal */
export const AuthorizeActionResponse$inboundSchema: z.ZodType<
  AuthorizeActionResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  redirectUrl: z.string(),
});

export function authorizeActionResponseFromJSON(
  jsonString: string,
): SafeParseResult<AuthorizeActionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AuthorizeActionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AuthorizeActionResponse' from JSON`,
  );
}
