/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b7fd21fddef4
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * One tool the caller must authorize before the agent can run.
 */
export type PlatformAuthenticationSuggestion = {
  /**
   * Human-readable tool name.
   */
  tool_name?: string | undefined;
  /**
   * Identifier to POST to `/tool-servers/{serverId}/auth` (Client API) with `returnUrl` in the request body to obtain an `authorizationUrl` to redirect the end user to.
   */
  server_id: string;
};

/** @internal */
export const PlatformAuthenticationSuggestion$inboundSchema: z.ZodType<
  PlatformAuthenticationSuggestion,
  z.ZodTypeDef,
  unknown
> = z.object({
  tool_name: z.string().optional(),
  server_id: z.string(),
});

export function platformAuthenticationSuggestionFromJSON(
  jsonString: string,
): SafeParseResult<PlatformAuthenticationSuggestion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformAuthenticationSuggestion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformAuthenticationSuggestion' from JSON`,
  );
}
