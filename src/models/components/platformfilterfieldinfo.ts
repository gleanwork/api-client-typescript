/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9d7ef9794894
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PlatformFilterOperator,
  PlatformFilterOperator$inboundSchema,
} from "./platformfilteroperator.js";

export type PlatformFilterFieldInfo = {
  /**
   * Field name usable in the `filters` array for `POST /api/search`.
   */
  field: string;
  /**
   * Open field-type string. Current values are `STRING` and `USER`. Clients must tolerate unknown values.
   *
   * @remarks
   */
  type: string;
  /**
   * Supported comparison operators for this field.
   */
  operators: Array<PlatformFilterOperator>;
  /**
   * Bounded, non-exhaustive facet values for this field. Omitted in catalog-only responses. Present as an array, including `[]`, when query-backed discovery executes. An empty array does not imply that no valid value exists.
   *
   * @remarks
   */
  values?: Array<string> | undefined;
};

/** @internal */
export const PlatformFilterFieldInfo$inboundSchema: z.ZodType<
  PlatformFilterFieldInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  field: z.string(),
  type: z.string(),
  operators: z.array(PlatformFilterOperator$inboundSchema),
  values: z.array(z.string()).optional(),
});

export function platformFilterFieldInfoFromJSON(
  jsonString: string,
): SafeParseResult<PlatformFilterFieldInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformFilterFieldInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformFilterFieldInfo' from JSON`,
  );
}
