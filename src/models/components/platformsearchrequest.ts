/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 649980857a72
 */

import * as z from "zod/v3";
import {
  PlatformFilter,
  PlatformFilter$Outbound,
  PlatformFilter$outboundSchema,
} from "./platformfilter.js";
import {
  PlatformTimeRange,
  PlatformTimeRange$Outbound,
  PlatformTimeRange$outboundSchema,
} from "./platformtimerange.js";

export type PlatformSearchRequest = {
  /**
   * The search query string. Supports inline operators such as `from:jane type:document app:confluence`. Inline operators are AND'd with structured `filters`.
   *
   * @remarks
   */
  query: string;
  /**
   * Number of results to return per page.
   */
  page_size?: number | undefined;
  /**
   * Opaque pagination token from a previous response's `next_cursor` field. Omit on the first request.
   *
   * @remarks
   */
  cursor?: string | null | undefined;
  /**
   * Restrict results to specific datasource identifiers returned by `GET /api/search/filters`. Scopes by datasource type and may include results from multiple instances of that type.
   *
   * @remarks
   */
  datasources?: Array<string> | undefined;
  /**
   * Structured filters applied to search results. Multiple values within a filter with `EQUALS` are OR'd; separate filters are AND'd. Conflicting constraints on the same field (for example, `type:document` in `query` and `type:spreadsheet` in a filter) return an empty result set. See `Filter.field` for built-in field names and operators. Other nonblank field names are accepted as custom filters without validation; behavior depends on your connected sources.
   *
   * @remarks
   */
  filters?: Array<PlatformFilter> | undefined;
  /**
   * Filter results to those last updated within this range.
   */
  time_range?: PlatformTimeRange | undefined;
};

/** @internal */
export type PlatformSearchRequest$Outbound = {
  query: string;
  page_size: number;
  cursor?: string | null | undefined;
  datasources?: Array<string> | undefined;
  filters?: Array<PlatformFilter$Outbound> | undefined;
  time_range?: PlatformTimeRange$Outbound | undefined;
};

/** @internal */
export const PlatformSearchRequest$outboundSchema: z.ZodType<
  PlatformSearchRequest$Outbound,
  z.ZodTypeDef,
  PlatformSearchRequest
> = z.object({
  query: z.string(),
  page_size: z.number().int().default(10),
  cursor: z.nullable(z.string()).optional(),
  datasources: z.array(z.string()).optional(),
  filters: z.array(PlatformFilter$outboundSchema).optional(),
  time_range: PlatformTimeRange$outboundSchema.optional(),
});

export function platformSearchRequestToJSON(
  platformSearchRequest: PlatformSearchRequest,
): string {
  return JSON.stringify(
    PlatformSearchRequest$outboundSchema.parse(platformSearchRequest),
  );
}
