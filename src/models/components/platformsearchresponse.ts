/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a12b46a101c0
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PlatformResult,
  PlatformResult$inboundSchema,
} from "./platformresult.js";

export type PlatformSearchResponse = {
  /**
   * Ordered list of search results.
   */
  results: Array<PlatformResult>;
  /**
   * Indicates whether additional pages of results are available.
   */
  hasMore: boolean;
  /**
   * Opaque token to pass as `cursor` in the next request.
   */
  nextCursor: string | null;
  /**
   * Platform-generated request ID for support correlation.
   */
  requestId: string;
};

/** @internal */
export const PlatformSearchResponse$inboundSchema: z.ZodType<
  PlatformSearchResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  results: z.array(PlatformResult$inboundSchema),
  has_more: z.boolean(),
  next_cursor: z.nullable(z.string()),
  request_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
    "next_cursor": "nextCursor",
    "request_id": "requestId",
  });
});

export function platformSearchResponseFromJSON(
  jsonString: string,
): SafeParseResult<PlatformSearchResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformSearchResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformSearchResponse' from JSON`,
  );
}
