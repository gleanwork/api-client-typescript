/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0b4832ec4dc7
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { PlatformSkill, PlatformSkill$inboundSchema } from "./platformskill.js";

export type PlatformSkillsListResponse = {
  /**
   * Skills available to the user.
   */
  skills: Array<PlatformSkill>;
  /**
   * Whether additional results are available.
   */
  hasMore: boolean;
  /**
   * Cursor for the next page, or null when no more results are available.
   */
  nextCursor: string | null;
  /**
   * Platform-generated request ID for support correlation.
   */
  requestId: string;
};

/** @internal */
export const PlatformSkillsListResponse$inboundSchema: z.ZodType<
  PlatformSkillsListResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  skills: z.array(PlatformSkill$inboundSchema),
  has_more: z.boolean(),
  next_cursor: z.nullable(z.string()),
  request_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
    "next_cursor": "nextCursor",
    "request_id": "requestId",
  });
});

export function platformSkillsListResponseFromJSON(
  jsonString: string,
): SafeParseResult<PlatformSkillsListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformSkillsListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformSkillsListResponse' from JSON`,
  );
}
