/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 35d75be7c013
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PlatformAuthenticationSuggestion,
  PlatformAuthenticationSuggestion$inboundSchema,
} from "./platformauthenticationsuggestion.js";
import {
  PlatformProblemDetail,
  PlatformProblemDetail$inboundSchema,
} from "./platformproblemdetail.js";

/**
 * Event type, also sent as the SSE `event` name.
 */
export const PlatformSkillSourcePreviewStreamErrorType = {
  Error: "ERROR",
} as const;
/**
 * Event type, also sent as the SSE `event` name.
 */
export type PlatformSkillSourcePreviewStreamErrorType = ClosedEnum<
  typeof PlatformSkillSourcePreviewStreamErrorType
>;

/**
 * Terminal failure event carrying the failure as a problem detail. Most GitHub authorization failures return HTTP 422 before the stream opens. If authorization fails after the stream opens, `error.code` is `tools_unauthorized` and `authentication_suggestions` is present.
 */
export type PlatformSkillSourcePreviewStreamError = {
  /**
   * Event type, also sent as the SSE `event` name.
   */
  type: PlatformSkillSourcePreviewStreamErrorType;
  /**
   * Error response following RFC 9457, extended with `code` and `documentation_url` for machine-readable classification and self-service remediation.
   *
   * @remarks
   */
  error: PlatformProblemDetail;
  /**
   * Present only when `error.code` is `tools_unauthorized`. Names the GitHub action pack to authorize, with the same retry flow as the 422 response.
   */
  authentication_suggestions?:
    | Array<PlatformAuthenticationSuggestion>
    | undefined;
};

/** @internal */
export const PlatformSkillSourcePreviewStreamErrorType$inboundSchema:
  z.ZodNativeEnum<typeof PlatformSkillSourcePreviewStreamErrorType> = z
    .nativeEnum(PlatformSkillSourcePreviewStreamErrorType);

/** @internal */
export const PlatformSkillSourcePreviewStreamError$inboundSchema: z.ZodType<
  PlatformSkillSourcePreviewStreamError,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: PlatformSkillSourcePreviewStreamErrorType$inboundSchema,
  error: PlatformProblemDetail$inboundSchema,
  authentication_suggestions: z.array(
    PlatformAuthenticationSuggestion$inboundSchema,
  ).optional(),
});

export function platformSkillSourcePreviewStreamErrorFromJSON(
  jsonString: string,
): SafeParseResult<PlatformSkillSourcePreviewStreamError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PlatformSkillSourcePreviewStreamError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformSkillSourcePreviewStreamError' from JSON`,
  );
}
