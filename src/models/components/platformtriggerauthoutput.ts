/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: df4f84046d5c
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Credential scheme.
 */
export const PlatformTriggerAuthType = {
  Bearer: "BEARER",
} as const;
/**
 * Credential scheme.
 */
export type PlatformTriggerAuthType = ClosedEnum<
  typeof PlatformTriggerAuthType
>;

/**
 * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve the existing credential; there is no in-place removal — recreate the trigger to remove auth.
 */
export type PlatformTriggerAuthOutput = {
  /**
   * Credential scheme.
   */
  type: PlatformTriggerAuthType;
};

/**
 * Optional caller credential sent as an HTTP auth header on each delivery, in addition to the HMAC signature. Lets the receiving endpoint authenticate the request. On update, omit `auth` to preserve the existing credential; there is no in-place removal — recreate the trigger to remove auth.
 */
export type PlatformTriggerAuth = {
  /**
   * Credential scheme.
   */
  type: PlatformTriggerAuthType;
  /**
   * Secret credential value. Write-only; never returned on reads.
   */
  secret: string;
};

/** @internal */
export const PlatformTriggerAuthType$inboundSchema: z.ZodNativeEnum<
  typeof PlatformTriggerAuthType
> = z.nativeEnum(PlatformTriggerAuthType);
/** @internal */
export const PlatformTriggerAuthType$outboundSchema: z.ZodNativeEnum<
  typeof PlatformTriggerAuthType
> = PlatformTriggerAuthType$inboundSchema;

/** @internal */
export const PlatformTriggerAuthOutput$inboundSchema: z.ZodType<
  PlatformTriggerAuthOutput,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: PlatformTriggerAuthType$inboundSchema,
});

export function platformTriggerAuthOutputFromJSON(
  jsonString: string,
): SafeParseResult<PlatformTriggerAuthOutput, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlatformTriggerAuthOutput$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlatformTriggerAuthOutput' from JSON`,
  );
}

/** @internal */
export type PlatformTriggerAuth$Outbound = {
  type: string;
  secret: string;
};

/** @internal */
export const PlatformTriggerAuth$outboundSchema: z.ZodType<
  PlatformTriggerAuth$Outbound,
  z.ZodTypeDef,
  PlatformTriggerAuth
> = z.object({
  type: PlatformTriggerAuthType$outboundSchema,
  secret: z.string(),
});

export function platformTriggerAuthToJSON(
  platformTriggerAuth: PlatformTriggerAuth,
): string {
  return JSON.stringify(
    PlatformTriggerAuth$outboundSchema.parse(platformTriggerAuth),
  );
}
