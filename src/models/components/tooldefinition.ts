/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e402d1078b0c
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ToolAnnotations,
  ToolAnnotations$inboundSchema,
} from "./toolannotations.js";

export type ToolDefinition = {
  /**
   * Tool server this tool belongs to.
   */
  serverId: string;
  /**
   * Name to invoke the tool by.
   */
  name: string;
  /**
   * Human-readable name of the tool.
   */
  displayName?: string | undefined;
  /**
   * What the tool does, as surfaced to a model.
   */
  description?: string | undefined;
  /**
   * JSON Schema for the tool's input parameters.
   */
  inputSchema?: { [k: string]: any } | undefined;
  /**
   * Behavioural hints for the tool, exactly as declared by the tool server. No
   *
   * @remarks
   * defaults are filled in — see the per-field notes for what an absent value means.
   *
   * Per the MCP specification, clients MUST treat these hints as untrusted unless
   * the tool server is itself trusted. They are advisory metadata, not an
   * authorization signal, and must not be the sole basis of a security decision.
   */
  annotations?: ToolAnnotations | undefined;
};

/** @internal */
export const ToolDefinition$inboundSchema: z.ZodType<
  ToolDefinition,
  z.ZodTypeDef,
  unknown
> = z.object({
  serverId: z.string(),
  name: z.string(),
  displayName: z.string().optional(),
  description: z.string().optional(),
  inputSchema: z.record(z.any()).optional(),
  annotations: ToolAnnotations$inboundSchema.optional(),
});

export function toolDefinitionFromJSON(
  jsonString: string,
): SafeParseResult<ToolDefinition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToolDefinition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToolDefinition' from JSON`,
  );
}
