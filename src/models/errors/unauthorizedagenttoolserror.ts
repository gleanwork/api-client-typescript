/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9cfc0ed0d054
 */

import * as z from "zod/v3";
import * as components from "../components/index.js";
import { GleanBaseError } from "./gleanbaseerror.js";

/**
 * Returned when the agent has tools the caller must authorize before the run can start. Each entry in `authenticationSuggestions` names one such tool; POST its `serverId` to `/tool-servers/{serverId}/auth` with `returnUrl` in the request body to obtain an `authorizationUrl` to redirect the end user to, then retry the run once OAuth completes.
 */
export type UnauthorizedAgentToolsErrorData = {
  /**
   * Human-readable description of the failure.
   */
  message: string;
  /**
   * One entry per tool the caller must authorize.
   */
  authenticationSuggestions: Array<components.ServerToolRequest>;
};

/**
 * Returned when the agent has tools the caller must authorize before the run can start. Each entry in `authenticationSuggestions` names one such tool; POST its `serverId` to `/tool-servers/{serverId}/auth` with `returnUrl` in the request body to obtain an `authorizationUrl` to redirect the end user to, then retry the run once OAuth completes.
 */
export class UnauthorizedAgentToolsError extends GleanBaseError {
  /**
   * One entry per tool the caller must authorize.
   */
  authenticationSuggestions: Array<components.ServerToolRequest>;

  /** The original data that was passed to this error instance. */
  data$: UnauthorizedAgentToolsErrorData;

  constructor(
    err: UnauthorizedAgentToolsErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.authenticationSuggestions = err.authenticationSuggestions;

    this.name = "UnauthorizedAgentToolsError";
  }
}

/** @internal */
export const UnauthorizedAgentToolsError$inboundSchema: z.ZodType<
  UnauthorizedAgentToolsError,
  z.ZodTypeDef,
  unknown
> = z.object({
  message: z.string(),
  authenticationSuggestions: z.array(
    components.ServerToolRequest$inboundSchema,
  ),
  request$: z.instanceof(Request),
  response$: z.instanceof(Response),
  body$: z.string(),
})
  .transform((v) => {
    return new UnauthorizedAgentToolsError(v, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
