/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 59c54074fd52
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GleanBaseError } from "../models/errors/gleanbaseerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { useGleanContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  buildClientToolsGetToolServerToolsQuery,
  ClientToolsGetToolServerToolsQueryData,
  prefetchClientToolsGetToolServerTools,
  queryKeyClientToolsGetToolServerTools,
} from "./clientToolsGetToolServerTools.core.js";
export {
  buildClientToolsGetToolServerToolsQuery,
  type ClientToolsGetToolServerToolsQueryData,
  prefetchClientToolsGetToolServerTools,
  queryKeyClientToolsGetToolServerTools,
};

export type ClientToolsGetToolServerToolsQueryError =
  | GleanBaseError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * Get tool definitions from a tool server.
 *
 * @remarks
 * Returns the name, description and JSON input schema for the named tools on the
 * specified tool server. Works for Glean's built-in tools, tool packs and MCP
 * servers.
 *
 * `toolNames` is required. Names that do not exist on the server are returned in
 * `notFound` rather than failing the request, so a single bad name does not force
 * callers into one-at-a-time retries. Matching is case-insensitive and treats `-`
 * and `_` as equivalent.
 *
 * Use `serverId=native` for Glean's built-in tools. Their names are matched
 * exactly.
 */
export function useClientToolsGetToolServerTools(
  serverId: string,
  toolNames: Array<string>,
  options?: QueryHookOptions<
    ClientToolsGetToolServerToolsQueryData,
    ClientToolsGetToolServerToolsQueryError
  >,
): UseQueryResult<
  ClientToolsGetToolServerToolsQueryData,
  ClientToolsGetToolServerToolsQueryError
> {
  const client = useGleanContext();
  return useQuery({
    ...buildClientToolsGetToolServerToolsQuery(
      client,
      serverId,
      toolNames,
      options,
    ),
    ...options,
  });
}

/**
 * Get tool definitions from a tool server.
 *
 * @remarks
 * Returns the name, description and JSON input schema for the named tools on the
 * specified tool server. Works for Glean's built-in tools, tool packs and MCP
 * servers.
 *
 * `toolNames` is required. Names that do not exist on the server are returned in
 * `notFound` rather than failing the request, so a single bad name does not force
 * callers into one-at-a-time retries. Matching is case-insensitive and treats `-`
 * and `_` as equivalent.
 *
 * Use `serverId=native` for Glean's built-in tools. Their names are matched
 * exactly.
 */
export function useClientToolsGetToolServerToolsSuspense(
  serverId: string,
  toolNames: Array<string>,
  options?: SuspenseQueryHookOptions<
    ClientToolsGetToolServerToolsQueryData,
    ClientToolsGetToolServerToolsQueryError
  >,
): UseSuspenseQueryResult<
  ClientToolsGetToolServerToolsQueryData,
  ClientToolsGetToolServerToolsQueryError
> {
  const client = useGleanContext();
  return useSuspenseQuery({
    ...buildClientToolsGetToolServerToolsQuery(
      client,
      serverId,
      toolNames,
      options,
    ),
    ...options,
  });
}

export function setClientToolsGetToolServerToolsData(
  client: QueryClient,
  queryKeyBase: [serverId: string, parameters: { toolNames: Array<string> }],
  data: ClientToolsGetToolServerToolsQueryData,
): ClientToolsGetToolServerToolsQueryData | undefined {
  const key = queryKeyClientToolsGetToolServerTools(...queryKeyBase);

  return client.setQueryData<ClientToolsGetToolServerToolsQueryData>(key, data);
}

export function invalidateClientToolsGetToolServerTools(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [serverId: string, parameters: { toolNames: Array<string> }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gleanwork/api-client",
      "tools",
      "getToolServerTools",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllClientToolsGetToolServerTools(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gleanwork/api-client", "tools", "getToolServerTools"],
  });
}
