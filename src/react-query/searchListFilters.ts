/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fb6d3135bc3a
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GleanBaseError } from "../models/errors/gleanbaseerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { useGleanContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";
import {
  buildSearchListFiltersQuery,
  prefetchSearchListFilters,
  queryKeySearchListFilters,
  SearchListFiltersQueryData,
} from "./searchListFilters.core.js";
export {
  buildSearchListFiltersQuery,
  prefetchSearchListFilters,
  queryKeySearchListFilters,
  type SearchListFiltersQueryData,
};

export type SearchListFiltersQueryError =
  | errors.PlatformProblemDetailError
  | GleanBaseError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * List search filters
 *
 * @remarks
 * Discover caller-visible datasources and common built-in filter fields that can be used with Platform Search. This is a best-effort common catalog, not an authoritative inventory of every field search may accept.
 * Without `query`, the response returns datasource rows and field metadata without executing search. With a nonblank `query`, exactly one `datasources` value is required and the response may include bounded, non-exhaustive facet values for matching public fields. Query-backed values preserve QE order and duplicate occurrences, ignore grouping metadata including nonempty `GroupName` (broad field/value semantics rather than exact UI group selection), and omit blank option strings. Query-backed discovery does not apply POST search typed-issue adaptation and does not expose warning objects. Rate limits return HTTP 429 with `Retry-After`. Temporary backend unavailability returns HTTP 503.
 */
export function useSearchListFilters(
  datasources?: Array<string> | undefined,
  query?: string | undefined,
  options?: QueryHookOptions<
    SearchListFiltersQueryData,
    SearchListFiltersQueryError
  >,
): UseQueryResult<SearchListFiltersQueryData, SearchListFiltersQueryError> {
  const client = useGleanContext();
  return useQuery({
    ...buildSearchListFiltersQuery(
      client,
      datasources,
      query,
      options,
    ),
    ...options,
  });
}

/**
 * List search filters
 *
 * @remarks
 * Discover caller-visible datasources and common built-in filter fields that can be used with Platform Search. This is a best-effort common catalog, not an authoritative inventory of every field search may accept.
 * Without `query`, the response returns datasource rows and field metadata without executing search. With a nonblank `query`, exactly one `datasources` value is required and the response may include bounded, non-exhaustive facet values for matching public fields. Query-backed values preserve QE order and duplicate occurrences, ignore grouping metadata including nonempty `GroupName` (broad field/value semantics rather than exact UI group selection), and omit blank option strings. Query-backed discovery does not apply POST search typed-issue adaptation and does not expose warning objects. Rate limits return HTTP 429 with `Retry-After`. Temporary backend unavailability returns HTTP 503.
 */
export function useSearchListFiltersSuspense(
  datasources?: Array<string> | undefined,
  query?: string | undefined,
  options?: SuspenseQueryHookOptions<
    SearchListFiltersQueryData,
    SearchListFiltersQueryError
  >,
): UseSuspenseQueryResult<
  SearchListFiltersQueryData,
  SearchListFiltersQueryError
> {
  const client = useGleanContext();
  return useSuspenseQuery({
    ...buildSearchListFiltersQuery(
      client,
      datasources,
      query,
      options,
    ),
    ...options,
  });
}

export function setSearchListFiltersData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      datasources?: Array<string> | undefined;
      query?: string | undefined;
    },
  ],
  data: SearchListFiltersQueryData,
): SearchListFiltersQueryData | undefined {
  const key = queryKeySearchListFilters(...queryKeyBase);

  return client.setQueryData<SearchListFiltersQueryData>(key, data);
}

export function invalidateSearchListFilters(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      datasources?: Array<string> | undefined;
      query?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gleanwork/api-client",
      "search",
      "listFilters",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllSearchListFilters(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gleanwork/api-client", "search", "listFilters"],
  });
}
