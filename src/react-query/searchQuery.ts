/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a67629da8040
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GleanCore } from "../core.js";
import { searchQuery } from "../funcs/searchQuery.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { GleanBaseError } from "../models/errors/gleanbaseerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { unwrapAsync } from "../types/fp.js";
import { useGleanContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type SearchQueryMutationVariables = {
  request: components.PlatformSearchRequest;
  options?: RequestOptions;
};

export type SearchQueryMutationData = components.PlatformSearchResponse;

export type SearchQueryMutationError =
  | errors.PlatformProblemDetailError
  | GleanBaseError
  | ResponseValidationError
  | ConnectionError
  | RequestAbortedError
  | RequestTimeoutError
  | InvalidRequestError
  | UnexpectedClientError
  | SDKValidationError;

/**
 * Search
 *
 * @remarks
 * Execute a search query and retrieve ranked results. This is the data retrieval variant of the search API and returns only results and pagination state. Structured filters accept exact public built-ins and free-form custom fields.
 * Successful responses always include a non-nullable `warnings` array (`[]` when empty). When results are incomplete for the requested datasource scope, the response remains HTTP 200 with `results`, `has_more`, and `next_cursor` preserved and a `results_incomplete` warning. Query outcomes that cannot be honored return HTTP 422 `unprocessable_query` and suppress results and cursor; invalid inline operators may include a nested `/query` `invalid_filter` issue. Backend work and audit logging may already have occurred before such a 422 replaces a result-bearing response. Structural and representability failures remain HTTP 400. Rate limits return HTTP 429 with `Retry-After`. Temporary backend unavailability returns HTTP 503.
 */
export function useSearchQueryMutation(
  options?: MutationHookOptions<
    SearchQueryMutationData,
    SearchQueryMutationError,
    SearchQueryMutationVariables
  >,
): UseMutationResult<
  SearchQueryMutationData,
  SearchQueryMutationError,
  SearchQueryMutationVariables
> {
  const client = useGleanContext();
  return useMutation({
    ...buildSearchQueryMutation(client, options),
    ...options,
  });
}

export function mutationKeySearchQuery(): MutationKey {
  return ["@gleanwork/api-client", "search", "query"];
}

export function buildSearchQueryMutation(
  client$: GleanCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: SearchQueryMutationVariables,
  ) => Promise<SearchQueryMutationData>;
} {
  return {
    mutationKey: mutationKeySearchQuery(),
    mutationFn: function searchQueryMutationFn({
      request,
      options,
    }): Promise<SearchQueryMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(searchQuery(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}
