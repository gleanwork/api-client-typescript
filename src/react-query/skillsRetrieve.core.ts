/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b293a018b942
 */

import {
  QueryClient,
  QueryFunctionContext,
  QueryKey,
} from "@tanstack/react-query";
import { GleanCore } from "../core.js";
import { skillsRetrieve } from "../funcs/skillsRetrieve.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { unwrapAsync } from "../types/fp.js";
export type SkillsRetrieveQueryData = components.PlatformSkillGetResponse;

export function prefetchSkillsRetrieve(
  queryClient: QueryClient,
  client$: GleanCore,
  skillId: string,
  options?: RequestOptions,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildSkillsRetrieveQuery(
      client$,
      skillId,
      options,
    ),
  });
}

export function buildSkillsRetrieveQuery(
  client$: GleanCore,
  skillId: string,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (context: QueryFunctionContext) => Promise<SkillsRetrieveQueryData>;
} {
  return {
    queryKey: queryKeySkillsRetrieve(skillId),
    queryFn: async function skillsRetrieveQueryFn(
      ctx,
    ): Promise<SkillsRetrieveQueryData> {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };

      return unwrapAsync(skillsRetrieve(
        client$,
        skillId,
        mergedOptions,
      ));
    },
  };
}

export function queryKeySkillsRetrieve(skillId: string): QueryKey {
  return ["@gleanwork/api-client", "skills", "retrieve", skillId];
}
