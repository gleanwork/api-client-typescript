/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 8a1dd7dcdfdd
 */

import {
  QueryClient,
  QueryFunctionContext,
  QueryKey,
} from "@tanstack/react-query";
import { GleanCore } from "../core.js";
import { skillsRetrieveVersion } from "../funcs/skillsRetrieveVersion.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { unwrapAsync } from "../types/fp.js";
export type SkillsRetrieveVersionQueryData =
  components.PlatformSkillVersionGetResponse;

export function prefetchSkillsRetrieveVersion(
  queryClient: QueryClient,
  client$: GleanCore,
  skillId: string,
  version: number,
  options?: RequestOptions,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildSkillsRetrieveVersionQuery(
      client$,
      skillId,
      version,
      options,
    ),
  });
}

export function buildSkillsRetrieveVersionQuery(
  client$: GleanCore,
  skillId: string,
  version: number,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<SkillsRetrieveVersionQueryData>;
} {
  return {
    queryKey: queryKeySkillsRetrieveVersion(skillId, version),
    queryFn: async function skillsRetrieveVersionQueryFn(
      ctx,
    ): Promise<SkillsRetrieveVersionQueryData> {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };

      return unwrapAsync(skillsRetrieveVersion(
        client$,
        skillId,
        version,
        mergedOptions,
      ));
    },
  };
}

export function queryKeySkillsRetrieveVersion(
  skillId: string,
  version: number,
): QueryKey {
  return [
    "@gleanwork/api-client",
    "skills",
    "retrieveVersion",
    skillId,
    version,
  ];
}
